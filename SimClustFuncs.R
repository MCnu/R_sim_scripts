

sim_clust_interdist <- function(pri_list, sec_list) {
  plen <- length(pri_list)
  slen <- length(sec_list)
  if (plen != slen) stop("lists must have equal length")
}

sim_clust_theta <- function(pri_list, sec_list) {
  plen <- length(pri_list)
  slen <- length(sec_list)
  if (plen != slen) stop("lists must have equal length")
}


sim_clust_magcor <- function(pri_list, sec_list) {
  plen <- length(pri_list)
  slen <- length(sec_list)
  if (plen != slen) stop("lists must have equal length")
}
